$(function(){
    sketch.draw = function(){
        sketch.fillStyle = '#222';
        sketch.fillRect(0, 0, size, size);

        var maxRadius = size / 2 - 20;
        var ballSize = size / 50;
        
        for(var r = 0; r <= 360; a++){

        }
    };
});
